[package]
name = "isometric-renderer"
version = "0.1.0"
edition = "2021"

[dependencies]
png = "0.17"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
